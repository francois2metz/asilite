<%= for hit <- @results["hits"]["hits"] do %>
  <div>
    <h1 class="h2"><a href="<%= hit["path"] %>"><%= hit["title"] %></a></h1>
    <p><%= hit["tease"] %></p>
  </div>
<% end %>
<a href="?start=<%= String.to_integer(@start) + 10 %>" class="btn btn-default btn-lg">Afficher une autre page</a>
